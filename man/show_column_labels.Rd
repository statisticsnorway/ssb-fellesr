% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datadoc_funksjoner.R
\name{show_column_labels}
\alias{show_column_labels}
\title{Vis kolonneetiketter ved utskrift av et datasett}
\usage{
show_column_labels(data)
}
\arguments{
\item{data}{Et datasett som kan inneholde variabler med
kolonneetiketter.}
}
\value{
Datasettet som ble oppgitt i \code{data}, med kolonneetikettene
registrert i variabelens \code{pillar}-attributt. Verdiene, kolonnenavnene
og de opprinnelige kolonneetikettene beholdes uendret.
}
\description{
Legger variabelens kolonneetikett til som en \code{pillar}-etikett, slik at
etiketten vises under kolonnenavnet når datasettet skrives ut som en
tibble.
}
\details{
Variabler som ikke har en kolonneetikett, beholdes uendret.

Kolonneetiketten for hver variabel hentes med
\code{\link[labelled:var_label]{labelled::var_label()}}. Dersom variabelen har en ikke-tom etikett,
lagres denne som elementet \code{label} i variabelens \code{pillar}-attributt.

\code{pillar}-attributtet brukes ved utskrift av tibble-objekter og påvirker
ikke verdiene i datasettet. Funksjonen endrer heller ikke variabelens
opprinnelige \code{label}-attributt.

Eksisterende elementer i \code{pillar}-attributtet beholdes. Dersom
attributtet allerede inneholder et element med navnet \code{label},
erstattes dette med variabelens gjeldende kolonneetikett.
}
\examples{
data <- tibble::tibble(
  kjoenn = c(1, 2, 1),
  alder = c(35, 42, 28)
)

labelled::var_label(data$kjoenn) <- "Kjønn"
labelled::var_label(data$alder) <- "Alder i år"

show_column_labels(data)

}
\seealso{
\code{\link[labelled:var_label]{labelled::var_label()}} for å hente og angi kolonneetiketter.
}
