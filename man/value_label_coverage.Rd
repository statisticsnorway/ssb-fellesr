% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datadoc_funksjoner.R
\name{value_label_coverage}
\alias{value_label_coverage}
\title{Beregn dekning av verdietiketter}
\usage{
value_label_coverage(data, labelled_only = FALSE)
}
\arguments{
\item{data}{Et datasett som skal undersøkes for verdietiketter.}

\item{labelled_only}{En logisk verdi som angir om bare variabler som har
minst én verdietikett skal inkluderes. Når verdien er \code{FALSE}, inkluderes
alle variabler i datasettet. Standardverdien er \code{FALSE}.}
}
\value{
En tibble med én rad per variabel og følgende kolonner:

\describe{
\item{\code{variable}}{Navnet på variabelen.}
\item{\code{has_value_labels}}{Logisk verdi som angir om variabelen har minst
én registrert verdietikett.}
\item{\code{n_values}}{Antall observerte, ikke-manglende verdier i variabelen.}
\item{\code{n_values_with_label}}{Antall observerte verdier som har en
tilhørende verdietikett.}
\item{\code{pct_values_with_label}}{Andel observerte verdier som har en
tilhørende verdietikett, angitt i prosent og avrundet til to desimaler.}
\item{\code{n_unique_values}}{Antall unike observerte, ikke-manglende verdier.}
\item{\code{n_unique_values_with_label}}{Antall unike observerte verdier som
har en tilhørende verdietikett.}
\item{\code{pct_unique_values_with_label}}{Andel unike observerte verdier som
har en tilhørende verdietikett, angitt i prosent og avrundet til to
desimaler.}
}
}
\description{
Beregner hvor stor andel av de observerte verdiene i hver variabel som har
en tilhørende verdietikett.
}
\details{
Funksjonen beregner både dekning blant alle observerte verdier og blant
unike observerte verdier.

Verdietikettene hentes med \code{\link[labelled:val_labels]{labelled::val_labels()}}. Manglende verdier
tas ikke med i beregningene.

En observert verdi regnes som merket dersom verdien finnes blant verdiene
som er registrert i variabelens verdietiketter.

\code{pct_values_with_label} beregnes på grunnlag av alle observerte verdier.
Dersom samme verdi forekommer flere ganger, teller hver forekomst separat.

\code{pct_unique_values_with_label} beregnes derimot på grunnlag av de unike
observerte verdiene. Hver forskjellig verdi teller derfor bare én gang,
uavhengig av hvor ofte den forekommer i datasettet.

Dersom en variabel ikke inneholder noen observerte verdier, settes den
aktuelle prosentandelen til \code{NA}.

Når \code{labelled_only = TRUE}, inkluderes bare variabler som returneres av
\code{\link[=vars_with_value_labels]{vars_with_value_labels()}}.
}
\examples{
data <- tibble::tibble(
  kjoenn = labelled::labelled(
    c(1, 2, 1, 3, NA),
    labels = c(
      Mann = 1,
      Kvinne = 2
    )
  ),
  alder = c(35, 42, 28, 51, 37)
)

value_label_coverage(data)

value_label_coverage(
  data,
  labelled_only = TRUE
)

}
\seealso{
\code{\link[=vars_with_value_labels]{vars_with_value_labels()}} for å finne variabler med verdietiketter,
\code{\link[=values_without_labels]{values_without_labels()}} for å finne observerte verdier uten
verdietikett og \code{\link[labelled:val_labels]{labelled::val_labels()}} for å hente verdietikettene
til en variabel.
}
