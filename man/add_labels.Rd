% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datadoc_funksjoner.R
\name{add_labels}
\alias{add_labels}
\title{Legg til kolonneetiketter fra Datadoc}
\usage{
add_labels(data, filsti, show_labels = TRUE)
}
\arguments{
\item{data}{Et datasett som skal få lagt til kolonneetiketter.}

\item{filsti}{En tekststreng med filstien til Parquet-filen som datasettet
er lest fra. Filstien brukes til å finne den tilhørende DataDoc-filen.}

\item{show_labels}{En logisk verdi som angir om kolonneetikettene også skal
vises ved utskrift av datasettet. Når verdien er \code{TRUE}, behandles
datasettet med \code{\link[=show_column_labels]{show_column_labels()}}. Standardverdien er \code{TRUE}.}
}
\value{
Datasettet som ble oppgitt i \code{data}, med kolonneetiketter lagt til
for variabler som finnes både i datasettet og i DataDoc-filen.
}
\description{
Legger til kolonneetiketter på variablene i et datasett basert på
variabelnavnene i den tilhørende DataDoc-filen.
}
\details{
Variabeloversikten hentes med \code{\link[=datadoc_variabeloversikt]{datadoc_variabeloversikt()}}. Verdiene i
kolonnen \code{name} brukes som kolonneetiketter, mens \code{short_name} brukes til
å koble etikettene til variablene i \code{data}.

Metadata for variabler som ikke finnes i \code{data}, ignoreres.

Kolonneetikettene legges til med \code{\link[labelled:var_label]{labelled::var_label()}}. Dersom
\code{show_labels = TRUE}, registreres etikettene i tillegg som
\code{pillar}-etiketter med \code{\link[=show_column_labels]{show_column_labels()}}, slik at de vises under
kolonnenavnene når datasettet skrives ut som en tibble.

Meldinger som oppstår når variabeloversikten hentes, undertrykkes.
Advarsler og feil undertrykkes ikke.

\code{show_labels} må være én enkelt logisk verdi og kan ikke være \code{NA}.
}
\examples{
\dontrun{
data_med_labels <- add_labels(
  data = personell,
  filsti = "data/personell.parquet"
)

data_med_labels <- add_labels(
  data = personell,
  filsti = "data/personell.parquet",
  show_labels = FALSE
)
}

}
\seealso{
\code{\link[=datadoc_variabeloversikt]{datadoc_variabeloversikt()}} for å hente variabelmetadata,
\code{\link[labelled:var_label]{labelled::var_label()}} for å legge til kolonneetiketter og
\code{\link[=show_column_labels]{show_column_labels()}} for å vise etikettene ved utskrift.
}
