% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datadoc_funksjoner.R
\name{add_labelled_columns}
\alias{add_labelled_columns}
\title{Legg til kolonner med verdietiketter}
\usage{
add_labelled_columns(data, variables = NULL, postfix = "_labelled")
}
\arguments{
\item{data}{Et datasett som inneholder én eller flere merkede variabler
med verdietiketter.}

\item{variables}{En karaktervektor med navn på variablene det skal
opprettes nye etikettkolonner for. Alle variablene må finnes i \code{data}
og være merkede variabler som gjenkjennes av \code{\link[haven:is.labelled]{haven::is.labelled()}}.
Dersom \code{NULL}, opprettes nye kolonner for alle merkede variabler i
\code{data}. Standardverdien er \code{NULL}.}

\item{postfix}{En tekststreng som legges til på slutten av navnet til
de nye variablene. Standardverdien er \code{"_labelled"}.}
}
\value{
Datasettet som ble oppgitt i \code{data}, med én ny kolonne for hver
valgt merket variabel. De opprinnelige variablene beholdes uendret.
De nye variablene er faktorer der verdietikettene brukes som
faktorverdier.
}
\description{
Oppretter nye kolonner med verdietiketter for merkede variabler,
uten å endre de opprinnelige variablene.
}
\details{
Funksjonen konverterer de valgte variablene med \code{\link[haven:as_factor]{haven::as_factor()}} og
\code{levels = "labels"}. Dette innebærer at de nye kolonnene inneholder
verdietikettene, mens de opprinnelige kodene beholdes i de opprinnelige
variablene.

Dersom \code{variables = NULL}, identifiseres alle merkede variabler i
datasettet ved hjelp av \code{\link[haven:is.labelled]{haven::is.labelled()}}. Dersom ingen merkede
variabler finnes, returneres datasettet uendret og det gis en advarsel.

Navnet på hver ny variabel består av det opprinnelige variabelnavnet
etterfulgt av verdien i \code{postfix}. Dersom for eksempel variabelen heter
\code{kjoenn} og \code{postfix = "_labelled"}, får den nye variabelen navnet
\code{kjoenn_labelled}.

Funksjonen gir en feil dersom:
\itemize{
\item \code{variables} ikke er \code{NULL} eller en karaktervektor med minst ett
variabelnavn.
\item én eller flere av variablene i \code{variables} ikke finnes i \code{data}.
\item én eller flere av variablene i \code{variables} ikke er merkede variabler.
\item én eller flere av de nye variablene allerede finnes i \code{data}.
\item \code{postfix} ikke er én enkelt tekststreng.
}

Det gis en advarsel dersom konverteringen introduserer nye
missing-verdier. Missing-verdier som allerede finnes i de opprinnelige
variablene utløser ikke advarselen. Nye missing-verdier kan blant annet
oppstå dersom enkelte verdier ikke har en tilhørende verdietikett.
}
\examples{
data <- data.frame(
  kjoenn = labelled::labelled(
    c(1, 2, 1),
    labels = c(
      Mann = 1,
      Kvinne = 2
    )
  ),
  region = labelled::labelled(
    c("01", "02", "01"),
    labels = c(
      Ost = "01",
      Vest = "02"
    )
  ),
  alder = c(30, 45, 52)
)

# Opprett etikettkolonner for alle merkede variabler
add_labelled_columns(
  data = data
)

# Opprett etikettkolonne for én bestemt variabel
add_labelled_columns(
  data = data,
  variables = "kjoenn"
)

# Opprett etikettkolonner for flere bestemte variabler
add_labelled_columns(
  data = data,
  variables = c(
    "kjoenn",
    "region"
  )
)

# Bruk et annet postfiks
add_labelled_columns(
  data = data,
  variables = "kjoenn",
  postfix = "_navn"
)

}
