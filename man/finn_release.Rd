% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versjonering.R
\name{finn_release}
\alias{finn_release}
\title{Finn en release fra logg basert på ulike kriterier}
\usage{
finn_release(
  release = NULL,
  dato = NULL,
  dato_start = NULL,
  dato_slutt = NULL,
  periode = NULL,
  arbeidsmappe,
  logg_fil = "versjonering_logg.json"
)
}
\arguments{
\item{release}{En karakterstreng som spesifiserer hvilken release som skal hentes. Hvis \code{release = "siste"}, returneres den nyeste releasen basert på dato. Kombiner med \code{periode} for å hente den siste releasen innenfor en spesifikk periode.}

\item{dato}{En karakterstreng som spesifiserer en eksakt dato eller dato med tid (f.eks. "2023", \code{"2023-01-01"} eller \code{"2023-01-01 12:00:00"}).
Hvis bare dato oppgis, returneres alle releaser fra denne dagen.}

\item{dato_start}{En karakterstreng som spesifiserer startdato for et datointervall (f.eks. "2023", \code{"2023-01-01"}). Brukes sammen med \code{dato_slutt} for å hente releaser innenfor et intervall.}

\item{dato_slutt}{En karakterstreng som spesifiserer sluttdato for et datointervall (f.eks. "2023", \code{"2023-01-31"}). Brukes sammen med \code{dato_start}.}

\item{periode}{En karakterstreng som spesifiserer en tidsperiode (f.eks. "2023", \code{"2023-Q1"}). Hvis \code{release = "siste"} er angitt, returneres den nyeste releasen innenfor den spesifiserte perioden.}

\item{arbeidsmappe}{En karakterstreng som representerer banen til arbeidsmappen der loggfilen ligger.}

\item{logg_fil}{En karakterstreng som representerer navnet på loggfilen. Standard er \code{"versjonering_logg.json"}.}
}
\value{
Returnerer en liste over releaser som samsvarer med kriteriene, eller \code{NULL} hvis ingen releaser ble funnet eller hvis loggfilen ikke eksisterer.
}
\description{
Funksjonen søker gjennom en loggfil for å finne en spesifikk release, siste release, alle releaser innenfor et datointervall, alle releaser for en gitt periode eller siste release innenfor en spesifisert periode.
}
\details{
Funksjonen leser inn en loggfil i JSON-format og søker etter en release basert på de oppgitte kriteriene.
Du kan søke etter:
\itemize{
\item Spesifikk release: Angi \code{release} for å hente en bestemt release.
\item Nyeste release: Angi \code{release = "siste"} for å hente den siste basert på tidsstempelet.
\item Nyeste release innenfor en spesifikk periode: Angi både \code{release = "siste"} og \code{periode} for å hente den nyeste releasen innenfor perioden.
\item Spesifikk dato: Angi \code{dato} for å hente releaser som ble logget på en gitt dato.
\item Dato-intervall: Angi både \code{dato_start} og \code{dato_slutt} for å hente releaser innenfor et bestemt tidsrom.
}

Hvis flere kriterier er oppgitt (f.eks. både \code{release} og \code{dato}), vil \code{release}-kriteriet ha prioritet.
}
\examples{
# Finn siste release
finn_release(release = "siste", arbeidsmappe = "prosjekt")

# Finn siste release i en spesifikk periode
finn_release(release = "siste", periode = "2021-Q1", arbeidsmappe = "prosjekt")

# Finn en spesifikk release
finn_release(release = "R1", arbeidsmappe = "prosjekt")

# Finn releaser fra en bestemt dato
finn_release(dato = "2023-01-01", arbeidsmappe = "prosjekt")

# Finn releaser innenfor et datointervall
finn_release(dato_start = "2023-01-01", dato_slutt = "2023-01-31", arbeidsmappe = "prosjekt")

}
\seealso{
\code{\link{logg_kjoring}} for å loggføre kjøringer i loggfilen.
}
