# Funksjon for aa hente initialene til paalogget bruker

Funksjonen `initialer_funk` henter initialene til brukeren som er
paalogget Jupyterlab/RStudio.

## Usage

``` r
initialer_funk(lastefil)
```

## Arguments

- lastefil:

  Navn av fil til å laste opp

## Value

Karaktervektor med initialer (vanligvis tre siffer)

## Examples

``` r
if (FALSE) { # \dontrun{
initialer_funk()
} # }
```
